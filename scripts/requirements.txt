# Dependencies of scripts/suggest-publications.py (a maintainer tool, not part of the build).
#   python3 -m venv .venv && .venv/bin/pip install -r scripts/requirements.txt
scholarly>=1.7,<2
requests>=2.31,<3
